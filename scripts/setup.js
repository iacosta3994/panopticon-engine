#!/usr/bin/env node\n\n/**\n * Setup helper - Creates directories and checks environment\n * Does NOT modify database architecture (uses Supabase)\n */\n\nconst fs = require('fs');\nconst path = require('path');\nconst crypto = require('crypto');\n\nconsole.log('\\n🚀 Panopticon Engine - Setup Helper\\n');\n\n// Create logs directory\nconst logsDir = path.join(__dirname, '..', 'logs');\nif (!fs.existsSync(logsDir)) {\n  fs.mkdirSync(logsDir, { recursive: true });\n  console.log('✅ Created logs directory');\n}\n\n// Check for .env file\nconst envPath = path.join(__dirname, '..', '.env');\nif (!fs.existsSync(envPath)) {\n  console.log('⚠️  No .env file found\\n');\n  console.log('To get started:');\n  console.log('  1. Copy .env.example to .env');\n  console.log('  2. Add your Supabase credentials (from supabase.com)');\n  console.log('  3. Add a JWT_SECRET (or generate with: openssl rand -base64 32)\\n');\n  console.log('See QUICKSTART.md for detailed setup instructions\\n');\n} else {\n  console.log('✅ .env file found\\n');\n}\n\nconsole.log('🎉 Setup check complete!\\n');\n